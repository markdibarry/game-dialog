{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "extensionHost",
            "preLaunchTask": "build",
            "request": "launch",
            "runtimeExecutable": "${execPath}",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}"
            ],
            "skipFiles": [
                "${workspaceFolder}/node_modules/**/*.js",
            ],
        }
    ]
}